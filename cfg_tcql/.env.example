OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=gpt-5-mini

# Optional: Time Cockpit API (Personal Access Token). If set, metadata is loaded into the prompt and generated TCQL is executed against the API.
# TIMECOCKPIT_API_KEY=your_time_cockpit_token
# FORCE_METADATA=1
